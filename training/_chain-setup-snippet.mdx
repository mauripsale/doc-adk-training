export const ChainSetup = ({module, project}) => (
  <blockquote>
    <p><strong>Environment setup:</strong> this lab modifies the <code>{project}</code> project from Module {module}, in place -- it's not a fresh project. If you're continuing the course in sequence, <code>cd</code> into that project (inside your <code>adk-training</code> directory) and you're all set. If you're starting fresh at this module, you'll need that project first -- Module {module}'s <code>lab-solution.md</code> has the complete <code>{project}</code> code to recreate it, then come back here. Note that <code>lab-solution.md</code> usually only shows the contents of <code>agent.py</code>; if that's not enough to get a working project (e.g. you still need to scaffold it with <code>uv</code>/<code>adk create</code>), check Module {module}'s <code>lab.md</code> for those setup steps.</p>
  </blockquote>
);
